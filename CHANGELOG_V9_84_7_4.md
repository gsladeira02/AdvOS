# AdvOS v9.84.7.4

- Corrigido o fluxo da câmera: `video.srcObject` é ligado após o elemento existir no DOM.
- Adicionado fallback para captura nativa em navegadores sem `getUserMedia`.
- Botões de câmera agora são explícitos `type=button`.
- “Enviar token por WhatsApp” -> “Receber token por WhatsApp”.
- Removida a frase “Não é necessário desenhar uma assinatura”.
