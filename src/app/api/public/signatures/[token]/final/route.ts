import { NextResponse } from 'next/server'; import { createAdminSupabase } from '@/lib/supabase/admin';
export async function GET(_req:Request,{params}:{params:Promise<{token:string}>}){const {token}=await params;const db=createAdminSupabase();const {data:r}=await db.from('signature_requests').select('law_firm_id,status,final_document_path').eq('public_token',token).maybeSingle();if(!r)return new NextResponse('Não encontrado.',{status:404});if(r.status!=='assinado'||!r.final_document_path)return new NextResponse('Documento ainda não foi assinado.',{status:409});const {data:file,error}=await db.storage.from('documents').download(r.final_document_path);if(error||!file)return new NextResponse('Arquivo indisponível.',{status:404});return new NextResponse(Buffer.from(await file.arrayBuffer()),{headers:{'Content-Type':'application/pdf','Content-Disposition':'inline; filename="documento-assinado.pdf"'}})}
