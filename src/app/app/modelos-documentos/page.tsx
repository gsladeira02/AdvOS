export const dynamic='force-dynamic';
import {getCurrentProfile} from '@/lib/current'; import {createAdminSupabase} from '@/lib/supabase/admin';
export default async function Modelos(){const {profile}=await getCurrentProfile();const db=createAdminSupabase();const [{data:templates},{data:clients}]=await Promise.all([db.from('document_templates').select('id,name,category,active,updated_at').eq('law_firm_id',profile.law_firm_id).order('name'),db.from('clients').select('id,name').eq('law_firm_id',profile.law_firm_id).order('name')]);return <div><div className="page-header"><div><h1>Modelos de documentos</h1><p>Modelos reutilizáveis com variáveis do cliente.</p></div></div><form action="/api/document-templates" method="post" className="card p-4 space-y-2.5"><input className="input" name="name" placeholder="Nome do modelo" required/><input className="input" name="category" placeholder="Categoria"/><textarea className="input min-h-40" name="content" placeholder="Use {{cliente}}, {{cpf}}, {{valor}}, {{data}}..." required/><button className="btn btn-primary">Salvar modelo</button></form><div className="mt-4 grid gap-2 md:grid-cols-2">{(templates||[]).map((x:any)=><div key={x.id} className="card p-4"><b>{x.name}</b><p className="text-xs text-slate-500">{x.category||'Sem categoria'} · {x.active?'Ativo':'Inativo'}</p><form action="/api/document-templates/generate" method="post" className="mt-3 grid gap-2"><input type="hidden" name="template_id" value={x.id}/><input className="input" name="title" placeholder="Título do documento" required/><select className="input" name="client_id" required><option value="">Selecione o cliente</option>{(clients||[]).map((c:any)=><option key={c.id} value={c.id}>{c.name}</option>)}</select><button className="btn btn-secondary">Gerar PDF para cliente</button></form></div>)}</div></div>}
