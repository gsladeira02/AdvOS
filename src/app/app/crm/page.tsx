export const dynamic='force-dynamic'; import {getCurrentProfile} from '@/lib/current'; import {createAdminSupabase} from '@/lib/supabase/admin'; import Link from 'next/link';
const stages=['novo','em_atendimento','qualificado','proposta','aguardando','convertido','perdido'];
export default async function CRM(){const {profile}=await getCurrentProfile();const db=createAdminSupabase();const {data}=await db.from('whatsapp_leads').select('id,name,phone,stage,service_interest,source_platform,campaign_name,ad_name,estimated_value,probability,responsible,next_action,next_action_at,loss_reason').eq('law_firm_id',profile.law_firm_id).order('updated_at',{ascending:false}).limit(150);return <div><div className="page-header"><div><h1>CRM jurídico</h1><p>Pipeline comercial com próxima ação, valor potencial e probabilidade.</p></div></div><div className="grid gap-3 sm:grid-cols-2 xl:grid-cols-4 mb-4">{['novo','qualificado','proposta','convertido'].map(s=><div key={s} className="card p-4"><p className="text-[10px] font-black uppercase text-slate-500">{s}</p><p className="text-2xl font-black">{(data||[]).filter((x:any)=>x.stage===s).length}</p></div>)}</div><div className="space-y-3">{(data||[]).map((x:any)=><form key={x.id} action="/api/crm/lead" method="post" className="card p-4"><input type="hidden" name="id" value={x.id}/><div className="grid gap-2.5 md:grid-cols-[1.4fr_1fr_1fr_1fr]"><div><b>{x.name||x.phone}</b><p className="text-xs text-slate-500">{x.service_interest||'Sem área'} · {x.source_platform||'WhatsApp'}</p>{x.campaign_name&&<p className="text-[10px] text-slate-400">{x.campaign_name} · {x.ad_name||''}</p>}</div><select className="input" name="stage" defaultValue={x.stage}>{stages.map(s=><option key={s} value={s}>{s}</option>)}</select><input className="input" name="estimated_value" defaultValue={x.estimated_value||0} placeholder="Valor"/><input className="input" name="probability" type="number" min="0" max="100" defaultValue={x.probability||0} placeholder="Probabilidade %"/><input className="input" name="responsible" defaultValue={x.responsible||''} placeholder="Responsável"/><input className="input md:col-span-2" name="next_action" defaultValue={x.next_action||''} placeholder="Próxima ação"/><input className="input" name="next_action_at" type="datetime-local" defaultValue={x.next_action_at?new Date(x.next_action_at).toISOString().slice(0,16):''}/><input className="input" name="loss_reason" defaultValue={x.loss_reason||''} placeholder="Motivo de perda (se perdido)"/><div className="md:col-span-4 flex items-center justify-between gap-2"><Link href={`/app/whatsapp?lead=${x.id}`} className="text-xs font-black text-[#075e54]">Abrir WhatsApp</Link><button className="btn btn-primary">Salvar CRM</button></div></div></form>)}</div></div>}
